// Redux contains Application State, generated by Reducers. That State is modified
// by calling Action Creators, which return Actions. Those Actions flow through
// Middleware and then to Reducers. When new set of State is produced, it flows
// into Containers which re-render themselves. 
